#!/usr/bin/env python3
# Phase B curated answers — Part 1: B01 Kubernetes, B02 Terraform, B03 AWS
# Each entry: (exact question, model answer, rubric, why asked)

ANSWERS = {
"B01": [
("Pod vs Deployment vs StatefulSet vs DaemonSet vs Job/CronJob — when do you choose each?",
 "Deployment: stateless replicas behind a Service — the default for web/API workloads. StatefulSet: stable identity, stable storage (PVC per replica), ordered scaling/updates — for databases, message brokers, anything with persistent identity. DaemonSet: exactly one pod per node — agents (node-exporter, kube-proxy, log collectors, CNI). Job: run-to-completion batch work (one-off); CronJob: scheduled batch (nightly backups, cleanup). Rule of thumb: stateless → Deployment; stateful with identity → StatefulSet; per-node daemon → DaemonSet; finishes → Job; on a schedule → CronJob.",
 "1 = names the types. 2 = maps each to a correct workload class. 3 = explains StatefulSet's stable identity/ordering and picks from real tradeoffs (e.g. 'DB in a Deployment' as an anti-pattern).",
 "Kubernetes fundamentals are 30% of B01 — the 'which object' question separates people who read docs from people who run workloads."),
("What are init containers for? Give a real example.",
 "Init containers run to completion BEFORE the main container starts, sequentially, in the same pod (shared network + volumes). Use cases: wait-for-dependency (e.g. wait for a DB to accept connections), fetch/transform data into a shared volume, set up permissions, run migration scripts once. Real example: an app pod that needs a config file generated from a template — an init container renders the file into an emptyDir volume, the main container reads it; or a 'wait-for-migrate' init that polls a migration Job's status before the app starts.",
 "1 = knows they run before the main container. 2 = explains sequencing + shared volumes. 3 = gives a real wait-for/seed example and the failure semantics (pod restarts on init failure).",
 "Init containers test whether you understand pod lifecycle and real startup ordering problems — a frequent whiteboard question."),
("requests vs limits — what's the QoS model and what happens when a pod exceeds its limits?",
 "requests = what the scheduler reserves (guaranteed capacity); limits = what the pod may NOT exceed. Three QoS classes: Guaranteed (requests == limits for all containers), Burstable (requests < limits), BestEffort (no requests/limits). CPU is compressible: exceeding the limit throttles (100ms quota windows), no kill. Memory is NOT compressible: exceeding the limit → OOMKill (the kernel kills a container). If limits are set but requests omitted, Kubernetes copies the limit to the request (making it Guaranteed) unless a LimitRange default applies; if requests are set but limits omitted, the limit stays unset (bounded by node capacity — Burstable). The answer to watch: memory over-limit = OOM kill, CPU over-limit = throttle, and QoS class determines eviction priority under node pressure.",
 "1 = defines requests vs limits. 2 = names Guaranteed/Burstable/BestEffort. 3 = explains CPU-throttle vs memory-OOMKill and eviction order by QoS.",
 "QoS is the single most common resource question — misconfiguring memory limits is the top cause of OOMKilled incidents."),
("A pod is CrashLoopBackOff — walk me through your diagnosis (describe, events, logs, probes)",
 "Order: `kubectl describe pod <name>` first — events show the LAST failure reason (ImagePullBackOff, OOMKilled, probe failure, invalid spec). Then `kubectl logs <pod> --previous` — the crashed container's last output (CrashLoopBackOff = the container starts then dies repeatedly; the current log may be empty). Check `kubectl get events --sort-by=.lastTimestamp`. Then categorize: (1) app error at startup → read logs; (2) OOMKilled → memory limit too low, `kubectl describe` shows 'Killed' reason; (3) liveness probe failing → app not ready for the probe threshold, check probe path/port; (4) init container failing → check init logs. Never restart blindly — the crash reason is in the previous log.",
 "1 = says 'check logs'. 2 = orders describe → logs --previous → events. 3 = distinguishes OOMKilled vs probe vs app-error and reads the right artifact per symptom.",
 "CrashLoopBackOff is the most common real-world k8s incident — this question tests the actual debugging loop, not memorized flags."),
("Control plane vs nodes — name the components and what each does (apiserver, scheduler, controller-manager, etcd | kubelet, kube-proxy, runtime)",
 "Control plane: etcd (the cluster's source of truth — key-value store of all objects), kube-apiserver (the only entry point — REST API, authn/authz, admission; everything talks to it), kube-scheduler (decides which node a new pod goes to, based on resource requests, affinities, taints), kube-controller-manager (runs controller loops — Deployment, ReplicaSet, endpoints, node-lifecycle etc., reconciling desired vs current). Nodes: kubelet (the node agent — talks to the apiserver, starts/stops containers, reports status, runs probes), kube-proxy (implements Service networking — iptables/ipvs rules for ClusterIP load-balancing), container runtime (containerd etc. — actually runs the containers). The key mental model: apiserver is the front door; everything else reads/writes through it.",
 "1 = names 2-3 components. 2 = lists all with one-line duties. 3 = explains apiserver-centrality (nothing bypasses it) and the kubelet→apiserver control loop.",
 "Control plane anatomy is the B01 opener — a flawless list with the apiserver-centric model signals real cluster experience."),
("What does cordon + drain do and why use a PodDisruptionBudget first?",
 "cordon marks a node unschedulable (no NEW pods) but leaves existing ones running. drain evicts all pods from a node (also cordons it), preparing for maintenance/decommission. Drain refuses to evict pods without a controller (bare pods) or with a local PV unless forced. PodDisruptionBudget (PDB): declares 'at least N replicas must remain available' (minAvailable or maxUnavailable) — drain respects PDBs and blocks eviction if it would violate them, so you don't lose all replicas of a service during node maintenance. Order: set the PDB → cordon → drain → maintenance → uncordon. Why PDB first: without it, a rolling node replacement can take a service fully down.",
 "1 = defines cordon/drain. 2 = explains PDB concept. 3 = gives the full sequence and the graceful-degradation reasoning.",
 "Maintenance questions reveal whether you've actually operated clusters — the PDB-first ordering is the senior tell."),
("Taints & tolerations vs nodeSelector vs nodeAffinity — when is each appropriate?",
 "nodeSelector: hard constraint — pod only lands on nodes with a matching label (simple, coarse). nodeAffinity: richer version — requiredDuringScheduling (hard) or preferredDuringScheduling (soft), with matchExpressions. Taints (on nodes) + tolerations (on pods): 'repel' — a node with a taint only accepts pods that tolerate it. Use taints to reserve node pools (GPU nodes tainted so only GPU workloads land there; control-plane nodes). Affinity is pull-based (pod asks for a node); taints are push-based (node refuses). Combine: dedicated pools = taint + toleration + (optionally) nodeAffinity to spread. Rule: exact label match → nodeSelector; richer conditions/soft preferences → nodeAffinity; keep workloads off a pool → taints/tolerations.",
 "1 = defines the three. 2 = contrasts hard/soft affinity. 3 = explains push vs pull model and the GPU/control-plane taint use case.",
 "Scheduling control is where k8s ops get practical — this distinguishes declarative config users from planners."),
("A node is unhealthy — how do you detect it and evacuate workloads safely?",
 "Detection: `kubectl get nodes` shows NotReady after the node controller's timeout (default ~40s to mark, ~5m to evict); `kubectl describe node` shows conditions (Ready, MemoryPressure, DiskPressure, PIDPressure) and heartbeats. kubelet reports status/leases; check node-exporter/kubelet metrics for CPU/mem/disk. Evacuate safely: 1) verify the node isn't part of a healthy pool doing a normal restart; 2) cordon it (no new pods); 3) drain with a PDB in place so eviction respects availability; 4) if the node is unreachable (apiserver can't talk to kubelet), pods are force-deleted after the eviction timeout and rescheduled elsewhere — that's the node-lifecycle controller's job. For a hard-down node, don't fight it: cordon, let controllers reschedule pods on remaining nodes, then investigate the node itself (SSH/console, kubelet logs, disk full).",
 "1 = checks kubectl get nodes. 2 = uses conditions + cordon/drain. 3 = explains the eviction-timeout path and PDB-aware draining.",
 "Node failure handling separates operators from deployers — the evacuation order is what's graded."),
("Cluster upgrades: how would you upgrade a self-managed cluster without downtime?",
 "Principle: never upgrade control plane and data plane at the same time; keep one version gap max (k8s supports n-2). Steps: 1) check upgrade notes/deprecated APIs (`kubectl convert` or kubeconform for manifests); 2) back up etcd (snapshot + restore drill) — this is your rollback; 3) upgrade control plane one node at a time (or in-place if single control node, accepting brief API unavailability); 4) verify apiserver + etcd health; 5) upgrade nodes: drain each node (with PDBs), upgrade kubelet/kubeadm, uncordon, move to the next; 6) roll out node-by-node so workloads stay available. Extra: upgrade a spare node first as a canary, watch workloads, then continue. Never 'upgrade everything at once' — the graceful path is sequential with verification gates.",
 "1 = says 'drain nodes'. 2 = mentions etcd backup + version gap. 3 = gives the full sequential control-plane-then-nodes flow with canary + rollback.",
 "Upgrade questions test operational maturity — the etcd-backup-first and one-version-gap answers are the senior markers."),
("Pod stuck Pending — enumerate every cause and how you'd confirm each (describe events first)",
 "Pending = scheduler hasn't placed it or can't. `kubectl describe pod` events first — the scheduler writes reasons there. Causes: 1) insufficient resources (requests > node capacity) → '0/5 nodes are available: insufficient cpu' — check requests vs allocatable, add nodes or lower requests; 2) unschedulable node (taints, cordon) → 'node(s) had untolerated taint' — tolerate or fix the taint; 3) nodeSelector/affinity matches nothing; 4) PVC not bound (PV pending) → event 'pod has unbound immediate PersistentVolumeClaims'; 5) image pull problems don't cause Pending (that's ImagePullBackOff after scheduling); 6) scheduler not running / no nodes. Confirm with `kubectl describe`, `kubectl get nodes`, `kubectl get pvc`, and check events for the exact reason string.",
 "1 = names 'not enough resources'. 2 = lists 3-4 causes with event strings. 3 = ties each cause to its exact event message and fix.",
 "Pending is the classic 'read the events' question — interviewers want the event-first discipline."),
("ImagePullBackOff vs CrashLoopBackOff vs OOMKilled — distinct diagnoses for each",
 "ImagePullBackOff: the container runtime can't pull the image — wrong tag/name, registry auth failure, registry unreachable, or (on kind/minikube) image not present locally. Fix: check `kubectl describe` events for the pull error, fix image reference/credentials. CrashLoopBackOff: image pulled fine, container starts then exits repeatedly — app error, bad config, missing dependency, or a probe killing it. Fix: `kubectl logs --previous`. OOMKilled: container exceeded its memory limit (or node memory) — the kernel killed it. Fix: `kubectl describe` shows 'OOMKilled' in lastState; raise the limit, reduce the app's memory, or add memory limits to neighbors. They can cascade: an image that starts fine but immediately dies on bad config is CrashLoopBackOff; if that process allocates memory it can become OOMKilled.",
 "1 = knows the three states exist. 2 = gives one cause each. 3 = distinguishes by evidence (events vs logs vs lastState) and handles cascades.",
 "This is the k8s triage trifecta — a favorite scenario question with a clear evidence-based answer."),
("Service not reachable from outside — check ordering: pod → endpoints → service → ingress → DNS → security group",
 "Walk layers bottom-up: 1) pod running? (`kubectl get pods`, describe, logs); 2) endpoints populated? (`kubectl get endpoints` — if empty, selector mismatch, or pod not Ready); 3) Service correct? (type, ports, selector — `kubectl describe svc` shows Endpoints field); 4) Ingress exists and routes? (host/path rules, TLS, ingress controller running); 5) DNS: does the name resolve (`nslookup`, `dig`, or test the Service IP directly with curl); 6) external: security group / firewall allows the port, LoadBalancer created, nodePort reachable. Key gotchas: empty endpoints from selector typos, Service pointing at non-Ready pods, ingress controller not deployed, SG not updated after adding a port.",
 "1 = checks pod logs. 2 = follows pod→svc→ingress order. 3 = uses endpoints as the selector check and covers DNS + security groups with the empty-endpoints gotcha.",
 "Connectivity debugging is the #1 real incident type — the ordered walk-through is exactly what operators do."),
("How do you debug DNS inside the cluster (CoreDNS, nslookup, /etc/resolv.conf)?",
 "Cluster DNS is CoreDNS — every pod's resolv.conf points at the CoreDNS ClusterIP (kube-dns service). Debug: 1) verify CoreDNS pods Running (`kubectl get pods -n kube-system -l k8s-app=kube-dns`); 2) run a debug pod: `kubectl run tmp --rm -it --image=busybox -- nslookup <svc>` — or `nslookup <svc>.<namespace>.svc.cluster.local` (fully-qualified, bypasses search domains); 3) check /etc/resolv.conf in a pod (search domain, nameserver = ClusterIP); 4) if external DNS fails but internal works → upstream forwarding issue in CoreDNS Corefile; 5) if only some namespaces fail → Service/headless service or endpoint issues; 6) check CoreDNS logs + metrics for QPS/errors. Common gotchas: DNS timeout from bad dnsPolicy, `resolv.conf` search domains causing NXDOMAIN on short names.",
 "1 = knows CoreDNS is the resolver. 2 = runs a debug pod with nslookup FQDN. 3 = distinguishes internal vs external resolution and checks CoreDNS logs/Corefile.",
 "DNS is the most-debugged k8s layer — a clean mental model here is a strong signal."),
("How does the scheduler pick a node — filtering, scoring, and what data does it use?",
 "Two phases: filtering (feasibility) then scoring (preference). Filtering removes nodes that can't run the pod: insufficient resources (requests vs allocatable), taints without tolerations, nodeSelector/affinity mismatch, port conflicts, volume zone/availability, node unschedulable (cordon). Scoring ranks remaining nodes: resource fit (most-requested ratio), topology spread, node affinity preferences, pod anti-affinity, image locality (node already has the image), node age/pressure. Data: node status (capacity, allocatable, conditions), pod specs (requests, affinity, tolerations), and the scheduler cache. Result: highest-scoring node wins (ties broken by round-robin). The scheduler never sees actual runtime usage — only requests.",
 "1 = says 'filters and scores'. 2 = names filter examples (resources, taints). 3 = explains requests-not-usage, image locality, and scoring tradeoffs.",
 "Scheduler internals test depth beyond kubectl apply — a strong answer implies you understand WHY pods land where they do."),
("Why is a pod stuck Pending — top causes and how events reveal the answer",
 "Pending means the scheduler hasn't assigned it. Top causes, each with its event line: insufficient CPU/memory ('0/N nodes are available: insufficient cpu'), tainted nodes with no toleration ('untolerated taint'), nodeSelector/affinity matching nothing, unbound PVC, scheduler disabled on the node (cordon), or no Ready nodes. Events (kubectl describe pod) print the exact scheduler reason — always read events before guessing. Also check `kubectl get events -n <ns> --sort-by=.lastTimestamp` for history. The fix path: read reason → fix the constraint (raise capacity, add toleration, fix selector, bind the PVC) → the pod schedules without manual intervention.",
 "1 = knows Pending = unscheduled. 2 = lists resource/taint/PVC causes. 3 = reads the exact event string and maps each to its fix.",
 "Duplicate-topic questions in one module are deliberate — interviewers ask 'Pending' repeatedly; the events-first answer is the consistent one."),
("HPA vs VPA vs Cluster Autoscaler vs Karpenter — what does each scale and what are the limits?",
 "HPA (Horizontal Pod Autoscaler): scales POD COUNT by metric (CPU/memory/custom/external) — the default for stateless services; limits: scale-to-zero not native, needs metrics server, metrics can lag. VPA (Vertical Pod Autoscaler): scales REQUESTS/LIMITS of existing pods (recommends and restarts pods) — for stateful or hard-to-shard workloads; limits: requires pod restart, mutating admission, no support with HPA on the same metric, burst can't exceed node. Cluster Autoscaler: scales NODES (adds/removes based on pending pods) at the cluster level — cloud-specific node groups; limits: slow (minutes), node group granularity, can't mix with Karpenter on same pool. Karpenter: node provisioning — fast, bin-packing, instance-type-aware, works with AWS/Azure; limits: newer, provider-specific. Layers compose: HPA handles pod count, CA/Karpenter handles node count.",
 "1 = names the four. 2 = maps each to pod vs node scaling. 3 = explains composition (HPA+CA), VPA restart cost, Karpenter bin-packing.",
 "Autoscaling is the layering test — 'what scales what' shows you understand the stack, not just the tools."),
("requests vs limits: why set requests low but limits high, or match them? What breaks if you omit them?",
 "Requests low + limits high (Burstable): good utilization — scheduler packs by requests, bursts allowed up to limits; risk: one pod can throttle/OOM-neighbor under contention. Requests == limits (Guaranteed): predictable, but wastes headroom (you reserve everything). Omitting requests: scheduler can overpack a node → CPU starvation, memory pressure, evictions; omitting limits: a runaway process can consume the whole node (memory → OOMKill of neighbors; CPU → everyone throttled). Practical defaults: always set both; requests = typical steady-state, limits = max acceptable; for latency-sensitive services match them (Guaranteed); for batch/elastic, low requests + high limits. Nothing breaks immediately on omission — it breaks at scale/contention.",
 "1 = defines requests/limits. 2 = contrasts Burstable vs Guaranteed. 3 = explains exactly what breaks when omitted (node starvation, neighbor OOM) and gives a default policy.",
 "This drills the earlier QoS question from the policy angle — what would you actually set in production."),
("Topology Spread Constraints and node affinity — when do you need them?",
 "Topology Spread Constraints: spread replicas across topology domains (zones, nodes) to survive a domain failure and balance load — needed for HA (e.g. 3 replicas across 3 AZs) and to avoid all replicas landing on one node. Node affinity: attract pods to specific node attributes (GPU pool, SSD nodes, region) — needed for placement requirements. Together they answer 'where do I WANT pods' (affinity) vs 'how do I spread them' (spread). Use spread when availability matters; use affinity when hardware/zone matters; they combine naturally (spread across zones, affinity to a GPU-labeled zone).",
 "1 = knows spread ≈ distribution. 2 = separates spread (HA) from affinity (placement). 3 = gives a concrete AZ-failure scenario where spread saved the day.",
 "HA questions distinguish checklist memorizers from architects — spread constraints are the production HA answer."),
("AI/GPU workloads: how do scheduling needs change for GPU inference (vLLM) vs CPU apps — node pools, bin-packing, limits?",
 "GPU nodes are expensive and scarce → isolate them with taints/tolerations so CPU apps never land there; use node pools per GPU type (A10 vs A100) with nodeAffinity. GPUs are requested via `nvidia.com/gpu` resource — a whole GPU is allocated (no sharing by default; MIG/Time-Slicing for sharing). Bin-packing matters: GPU is the scarce resource, so fit as many models as possible per node (vLLM's continuous batching makes this efficient) — Karpenter/CA with GPU-aware bin-packing. Limits: set memory limits carefully — vLLM preallocates KV cache; OOM kills are disruptive. Requests == limits for GPU pods (Guaranteed) to avoid oversubscription. Consider topology: NVLink/NUMA affinity for multi-GPU inference.",
 "1 = says 'GPU nodes are special'. 2 = explains nvidia.com/gpu + taints. 3 = discusses bin-packing, MIG/sharing, KV-cache memory, and pool isolation.",
 "GPU inference scheduling is the emerging premium k8s question — India job data flags it as a differentiator."),
("How do RBAC roles, RoleBindings, ClusterRoles, and ClusterRoleBindings differ? Give a least-privilege example",
 "Role: namespaced permission set (verbs + resources + apiGroups). RoleBinding: binds a Role to subjects (users/groups/SAs) WITHIN a namespace. ClusterRole: cluster-wide permission set (or reusable across namespaces). ClusterRoleBinding: binds cluster-wide. Least-privilege example: give a CI service account read-only access to one namespace's pods + deployments: Role {apiGroups: [''], resources: ['pods','deployments'], verbs: ['get','list','watch']} in ns 'ci', RoleBinding {subjects: [ServiceAccount 'ci-bot'], roleRef: that Role}. Use ClusterRoles only when truly cluster-scoped (node ops, cluster-admin, or a Role reused via RoleBinding across namespaces).",
 "1 = knows Role vs ClusterRole. 2 = explains the binding pair + namespaces. 3 = writes a correct minimal example and warns against cluster-admin defaults.",
 "RBAC is the security fundamentals question — least-privilege writing is the graded skill."),
("ServiceAccounts: why do pods need one and what happens with automountServiceAccountToken?",
 "Every pod has a ServiceAccount — its identity when talking to the API server. The apiserver authenticates via a JWT that the kubelet mounts at /var/run/secrets/kubernetes.io/serviceaccount/token, plus CA and namespace. Default SA is 'default' with no permissions (except token review) — but the token is still mounted, so any pod can authenticate as its SA. automountServiceAccountToken: false on the pod or SA disables the mount — do this for pods that never call the API (reduces token exposure; supply-chain attacks often steal these tokens). For pods that DO need API access, create a dedicated SA + RoleBinding with the minimum verbs. Also: projected token with audience + expiry is the modern pattern; OIDC federation lets workloads exchange k8s tokens for cloud creds.",
 "1 = knows pods get a token. 2 = explains default SA + automount. 3 = covers dedicated SAs, token rotation/projection, and disabling automount for API-free pods.",
 "SA token theft is a real attack path — this question tests security awareness beyond RBAC syntax."),
("Pod Security Standards (baseline vs restricted) — how do you enforce them?",
 "Pod Security Standards (PSS) define three profiles: privileged (no restrictions), baseline (defaults: no privileged containers, no hostPID/IPC/network, no hostPath except whitelist, drop ALL capabilities, no hostPorts, seccomp not required), restricted (tightest: read-only root FS, seccomp RuntimeDefault, no allowPrivilegeEscalation, capabilities limited to NET_BIND_SERVICE, non-root + specific runAsUser). Enforce via Pod Security Admission (PSA) — the built-in admission controller — with labels on namespaces: pod-security.kubernetes.io/enforce=restricted (warn/audit are softer modes). PSA replaces the deprecated PodSecurityPolicy (PSP). Enforcement happens at admission: a violating pod is rejected (enforce), warned, or audited. For GitOps: keep namespaces labeled; run most workloads under restricted, escalate to baseline only when needed.",
 "1 = knows PSS exists. 2 = contrasts baseline vs restricted. 3 = explains PSA labels + enforce/warn/audit and the PSP deprecation.",
 "Pod security is a must-know in 2026 — PSA labels are the practical enforcement mechanism."),
("Secrets: how are they stored in etcd and what are the real-world options (encryption at rest, External Secrets)?",
 "By default secrets are base64-encoded in etcd — NOT encrypted (base64 ≠ encryption; anyone with etcd access reads them). First real option: enable encryption at rest — kube-apiserver encrypts secrets in etcd with AES-GCM/KMS keys (--encryption-provider-config; KMS v2 with cloud KMS is the modern way). Second: reduce what's in etcd — use External Secrets Operator (syncs from Vault/AWS Secrets Manager/GCP SM into k8s Secrets), SOPS (encrypts secret manifests in Git, decrypted at apply), or Sealed Secrets (public-key encrypt manifests; only the cluster's controller can decrypt). Layering: encrypt at rest (defense in depth) + don't store raw secrets in Git (SOPS/ESO) + RBAC on secret access + audit logs.",
 "1 = knows base64 ≠ encryption. 2 = mentions encryption-at-rest config. 3 = explains ESO/SOPS/Sealed Secrets tradeoffs and the layered approach.",
 "Secret handling is an instant credibility check — 'base64 is not encryption' is the line they're listening for."),
("How do NetworkPolicies work — pod selectors, ingress/egress, default-deny vs allow-all?",
 "NetworkPolicy is an object that filters pod-to-pod traffic at the CNI level (Calico/Cilium implement it; some CNIs don't). Structure: podSelector (which pods), policyTypes (Ingress/Egress), ingress rules (from: podSelector/namespaceSelector/ipBlock + ports), egress rules (to: + ports). Semantics: empty selector matches ALL pods in the namespace; rules are OR'd; the default is allow-all unless a policy selects the pod — so the pattern is 'default-deny by selecting everything, then allow specific flows'. Critical gotchas: policy must be in the same namespace; no policy = allow all; DNS egress needs an explicit rule (allow egress to CoreDNS IP/namespace:53) or everything breaks; cluster-scoped services and nodePorts need ipBlock rules.",
 "1 = knows it filters pod-to-pod. 2 = explains podSelector + ingress/egress. 3 = covers default-deny pattern, DNS gotcha, and CNI dependency.",
 "NetworkPolicy is the zero-trust microsegmentation primitive — the DNS gotcha is what makes it a senior question."),
("Istio vs Linkerd: sidecar model, mTLS, traffic shifting — when would you adopt a mesh?",
 "Both inject a sidecar proxy into each pod. Istio (Envoy): full-featured — mTLS, traffic shifting (weighted canary, fault injection), rich observability (distributed tracing, metrics), gateway-based ingress, request routing by header. Heavy: ~30-80MB+ memory per sidecar, adds latency. Linkerd (its own Rust-based proxy): lighter (~10-20MB), simpler, fast — mTLS, golden metrics (success rate, latency), retries/timeouts, some traffic split; less deep routing. Adopt a mesh when you need: automatic mTLS everywhere (zero-trust), consistent metrics/tracing across many services, or canary/weighted routing as a standard practice. Skip it for small clusters, or when NetworkPolicies + app-level TLS + existing tracing suffice — the cost (memory, latency, ops) rarely pays off under ~20-30 services.",
 "1 = knows both are sidecar meshes. 2 = contrasts weight/features. 3 = gives adoption criteria (mTLS everywhere, service count) and the cost math.",
 "Service mesh is a premium B01 topic in the India data — the 'when is it overkill' half is the differentiator."),
("What's the cost of a service mesh (latency, ops, resource)? When is it overkill?",
 "Costs: resource — Istio sidecar ~0.5-1 vCPU + 50-100MB per pod (Linkerd ~half); latency — every packet crosses the sidecar (L4 proxy adds ~0.5-2ms p99, plus TLS overhead); ops — upgrading the mesh, certificate rotation, debugging 'why is my traffic different', sidecar lifecycle, mesh config drift; complexity — control plane upgrades, ambient/CNI modes to learn. Overkill when: small fleet (<~20 services), single region, no compliance demand for universal mTLS, existing observability is fine, or teams lack the SRE capacity to run the control plane. The honest senior answer: start with NetworkPolicies + app-level mTLS + OpenTelemetry; add a mesh only when a concrete need (per-service mTLS, weighted canary at scale, mesh-wide tracing) justifies the tax.",
 "1 = mentions latency/resource cost. 2 = quantifies sidecar overhead. 3 = gives the threshold logic (service count, mTLS need) and a cheaper alternative path.",
 "Cost-of-mesh is the counterweight to hype — interviewers use it to test judgment."),
("How do you enforce 'no pod-to-pod except API' with NetworkPolicies?",
 "Goal: pods may only talk to the API service (and DNS). Pattern: 1) default-deny all ingress/egress per namespace: a NetworkPolicy selecting all pods (podSelector: {}) with policyTypes: [Ingress, Egress] and no rules; 2) allow egress to DNS: to: {namespaceSelector: kube-system, podSelector: kube-dns} port 53 TCP/UDP (and optionally the API Service's pods); 3) allow ingress to the API pods only: podSelector matching api pods, from: {} (all) on the API port — or scope tighter (from: frontend pods); 4) allow egress from API pods to DB/redis etc. as needed. Test with a debug pod: `kubectl exec` curl should fail to everything except allowed targets. Remember: NetworkPolicies need a CNI that implements them (Cilium/Calico); default allow-all applies where unsupported.",
 "1 = says 'use NetworkPolicy'. 2 = writes default-deny + allow specific. 3 = includes the DNS egress rule and testing step — the two gotchas that break real enforcement.",
 "This is the zero-trust exercise — the DNS egress gotcha is what separates written answers from deployed ones."),
("What is a CRD and how does the Operator pattern extend Kubernetes?",
 "CRD (CustomResourceDefinition): declares a new API resource type (e.g. 'PostgresCluster') that the apiserver accepts and stores — it gives you declarative config for domain objects. On its own a CRD is just data storage; the Operator pattern adds a controller: a custom control loop that watches CRD objects and reconciles the cluster to match their spec (like the built-in controllers do for Deployments). Operator = CRD + controller (usually via controller-runtime/Operator SDK). It extends k8s from a container orchestrator into a platform where ANY system (databases, certs, message queues) is managed declaratively with self-healing.",
 "1 = knows CRD = custom resource. 2 = explains CRD + controller = Operator. 3 = contrasts with plain manifests and gives the self-healing payoff.",
 "The CRD+controller pairing is the definitional answer — half the module tests this exactly."),
("Explain the reconcile loop — what does the controller do when desired != current?",
 "Reconcile loop: watch desired state (from the API object, e.g. Deployment.spec.replicas: 3) → observe current state (ReplicaSet count) → diff → take action (create/scale/delete) → re-observe until they match, forever. When desired != current, the controller computes the minimal corrective action and re-applies; it's event-driven (watches) plus resync (periodic full re-check) for self-healing. Key properties: declarative (it doesn't 'get told' what to do, it derives it from the diff), idempotent (re-running the same diff is a no-op), and retry-on-error (backoff + exponential; errors surface in object status). Requeue with backoff is the standard failure path.",
 "1 = says 'watch + fix'. 2 = explains diff-driven action + retry. 3 = covers events + resync, idempotency, and status reporting.",
 "The reconcile loop is the heart of controllers — the diff-and-act mental model is what's graded."),
("Give examples of well-known Operators (Prometheus, ArgoCD, cert-manager) and what they automate",
 "Prometheus Operator: watches Prometheus/ServiceMonitor CRs — deploys and configures Prometheus servers, scrapes from ServiceMonitors, alerts via AlertmanagerConfig — so monitoring is declarative. ArgoCD: the GitOps engine — Application CRs point at Git repos; the controller syncs cluster state to Git (drift detection + auto-sync). cert-manager: Certificate/Issuer CRs — automatically obtains and renews TLS certs (Let's Encrypt via HTTP-01/DNS-01, or internal CA), injecting secrets + rotation before expiry. Pattern: each turns a previously manual, brittle operation (install/configure monitor, deploy app, get cert) into a declarative, self-healing resource.",
 "1 = names the three. 2 = states what each automates. 3 = extracts the shared pattern (CRD + controller replacing manual ops).",
 "Concrete operator examples prove you've seen the ecosystem, not just read about CRDs."),
("When would you write an Operator vs use plain manifests + ArgoCD?",
 "Use plain manifests + ArgoCD for: stateless apps, config, most services — declarative deploy with GitOps is enough; the controller already gives self-healing. Write an Operator when you need domain-specific lifecycle logic that manifests can't express: databases (backup/restore, scaling, failover), certificates (renewal timing), message brokers (rebalancing), anything with stateful, multi-step, ordered operations. Signals: you find yourself writing cron jobs to fix drift, shell scripts in CI mutating cluster state, or imperative steps that must run in order — that's a controller's job. Operator cost: significant (controller framework, RBAC, testing, versioning) — start with manifests + Helm/Kustomize + ArgoCD, escalate to an Operator only for genuine stateful lifecycle.",
 "1 = knows Operators are for stateful stuff. 2 = contrasts GitOps for stateless vs Operator for lifecycle. 3 = gives the smell signals (drift-fixing scripts) and cost framing.",
 "This is the judgment question — knowing when NOT to build an operator is the senior answer."),
("minikube vs kind vs a managed cluster (EKS/AKS/GKE) — when would you choose each?",
 "minikube: single-node, batteries-included (addons: ingress, dashboard, metrics-server), good for learning + local dev with a UI; heavier than kind, VM-based by default. kind: Kubernetes IN Docker — multi-node clusters on Docker, fastest to start, great for CI and testing controllers/operators (runs in containers). Managed (EKS/AKS/GKE): production-like — real cloud networking/LBs, managed control plane, node autoscaling — use for anything you're testing that involves cloud integration (IAM, ALB, spot, Karpenter), staging, and production. Choice: local experiments + learning → minikube/kind; testing controllers in CI → kind; anything cloud-integrated or real → managed.",
 "1 = names the options. 2 = picks by use case (learn vs CI vs prod). 3 = explains the cloud-integration gap (why kind can't test IAM/ALB).",
 "Environment choice shows whether you understand the gap between local and production k8s."),
("How do you keep a local cluster from eating your laptop (resources, ephemeral)?",
 "Give it limits: minikube with --cpus 2 --memory 4g (or kind with container resource limits); set Docker's memory limit so the cluster can't consume everything. Keep it ephemeral: create/destroy per experiment (`minikube delete` / `kind delete cluster`); don't let dev clusters accumulate state. Watch node pressure: `kubectl top nodes`, `docker stats`; pause or delete when idle. Avoid running heavyweight workloads locally (GPU, big DBs) — use cloud for those. Standard config: 2 vCPU/4GB for the VM, namespaces per experiment, delete when done.",
 "1 = knows it can eat RAM. 2 = sets explicit resource flags. 3 = adds the ephemeral/delete-per-experiment discipline and monitoring.",
 "A practical question that screens for people who've actually run clusters locally — and hit OOM."),
("What's the fastest way to recreate a clean cluster for an experiment?",
 "kind: `kind create cluster --name exp && kind delete cluster` — seconds, container-based, reproducible; ideal for fast experiments. minikube delete/create is also quick but slower (VM). For a specific version: `kind create cluster --image kindest/node:v1.31.0`. For GitOps experiments, pair with ArgoCD install script. The key habit: script the whole thing — a `make cluster-up` target that creates kind + installs CNI + ingress + cert-manager, and `make cluster-down` — so 'clean' is one command, not a 20-minute manual teardown.",
 "1 = says 'delete and recreate'. 2 = names kind and why. 3 = describes a scripted up/down workflow with version pinning.",
 "Recreate-speed tests operational taste — a scripted make target is the production answer."),
("Control plane vs nodes: apiserver, scheduler, controller-manager, etcd | kubelet, kube-proxy, container runtime",
 "Control plane: etcd (source of truth), apiserver (sole API front door, authn/authz/admission), scheduler (filters+scores node placement), controller-manager (reconcile loops: deployments, nodes, endpoints). Nodes: kubelet (node agent: runs containers, probes, reports status), kube-proxy (Service networking via iptables/ipvs), container runtime (containerd — executes containers). Mental model: apiserver is the only writer to etcd; kubelet is the only thing on the node that talks to the apiserver; everything else is a controller or data plane.",
 "1 = lists half. 2 = full list with duties. 3 = apiserver-central model + kubelet-only-node-agent insight."),
("Workloads: Pod vs Deployment vs StatefulSet vs DaemonSet vs Job/CronJob; init containers; requests vs limits (QoS)",
 "Stateless replicas → Deployment; stable identity/storage → StatefulSet; one-per-node agents → DaemonSet; run-to-completion → Job; scheduled → CronJob. Init containers run before main, sequentially, share volumes (wait-for-dependency, data prep). requests = scheduler reservation, limits = ceiling; QoS: Guaranteed (equal), Burstable (req<lim), BestEffort (none); CPU over-limit throttles, memory over-limit OOMKills; omission → overpacking/evictions at scale.",
 "1 = workload mapping only. 2 = adds init containers + QoS classes. 3 = ties omission consequences and throttling vs kill."),
("Scheduling: nodeName vs nodeSelector vs nodeAffinity; why a pod stays Pending (kubectl describe, events)",
 "nodeName: hard, direct pin (bypasses scheduler — used by DaemonSets); nodeSelector: simple label match; nodeAffinity: rich (required/preferred, matchExpressions). Pending = scheduler can't place: insufficient resources, taints, selector/affinity, unbound PVC, cordon — all visible in `kubectl describe pod` events; read events first.",
 "1 = defines the three. 2 = adds required vs preferred. 3 = ties each Pending cause to its event line."),
("Maintenance: cordon/drain (PDB minAvailable), taints/tolerations",
 "cordon = no new pods; drain = evict all (respects PDBs — minAvailable/maxUnavailable guarantee availability during eviction; refuses bare pods/local PV). taints repel pods without matching tolerations — reserve pools (GPU, control-plane). Sequence for node maintenance: set PDB → cordon → drain → work → uncordon.",
 "1 = defines cordon/drain. 2 = explains PDB role. 3 = full sequence + taint pool reservation."),
("Networking: service types (ClusterIP/NodePort/LoadBalancer), Ingress (L7, default backend, TLS secretName), NetworkPolicies",
 "ClusterIP = internal VIP (default); NodePort = port on every node; LoadBalancer = cloud LB → NodePort. Ingress = L7 routing (host/path), default backend for 404s, TLS via secretName. NetworkPolicy = pod-level firewall (podSelector, ingress/egress, default-deny) — implementer is the CNI. Debug order: pod → endpoints → service → ingress → DNS → SG.",
 "1 = lists service types. 2 = adds Ingress TLS/default backend. 3 = NetworkPolicy default-deny + DNS egress gotcha."),
("Storage: PVC/PV/StorageClass, CSI, emptyDir vs hostPath vs volumes",
 "PVC = request for storage; PV = the provisioned volume; StorageClass = dynamic provisioning (AWS EBS gp3, etc.) with reclaim policies. CSI = standard plugin interface (EBS/EFS/azuredisk drivers). Volume types: emptyDir = ephemeral pod-scratch; hostPath = node directory (careful: couples pod to node); cloud volumes = durable, AZ-bound. StatefulSets bind one PVC per replica — storage is per-AZ, so zone affinity matters.",
 "1 = PVC/PV/SC basics. 2 = CSI + dynamic provisioning. 3 = emptyDir vs hostPath vs cloud tradeoffs and AZ coupling."),
("Config: ConfigMap vs Secret, secretKeyRef, envFrom",
 "ConfigMap = non-sensitive config (env vars, files, volumes); Secret = sensitive (same mechanics, base64 + optional encryption-at-rest). Mount via envFrom (all keys as env), valueFrom secretKeyRef (one key), or volumes. Changes to CMs/secrets don't auto-restart pods — need rollout restart or a controller that watches them.",
 "1 = CM vs Secret difference. 2 = envFrom/secretKeyRef mechanics. 3 = the no-auto-restart gotcha and encryption-at-rest."),
("Logging: node agent vs sidecar, EFK/ELK stack",
 "Two collection models: node-level daemon (Fluentd/Fluent Bit/Filebeat reads container logs from /var/log/containers) vs sidecar (per-pod log shipper — needed for multi-format, structured, or app-level processing). EFK/ELK: shippers → Elasticsearch (index) → Kibana (UI); heavy (JVM). Loki alternative: label-based, cheaper, pairs with Grafana + Promtail. Consider log volume, retention, and cost per GB before choosing.",
 "1 = knows EFK/ELK. 2 = contrasts node-agent vs sidecar. 3 = adds Loki tradeoff + volume/retention planning."),
("Monitoring: Prometheus (server, client libraries, pushgateway, exporters, alertmanager), Grafana",
 "Prometheus pulls metrics from exporters/scrape targets (node-exporter, kube-state-metrics, cAdvisor, app /metrics); Alertmanager handles dedup/grouping/routing of alerts (PagerDuty, Slack); pushgateway for short-lived batch jobs (push workaround); recording rules precompute expensive queries. Grafana = dashboards/alerts UI over Prometheus + Loki + Tempo (three pillars). The pipeline: instrument (client libs) → scrape → store → query/alert → visualize.",
 "1 = names Prometheus + Grafana. 2 = explains pull model + exporters. 3 = adds Alertmanager routing, recording rules, pushgateway caveats."),
("Security: RBAC, namespaces (blue/green, multi-team), admission controllers, audit logs, PodSecurity",
 "RBAC: Roles/ClusterRoles + Bindings, least privilege per SA. Namespaces: isolation + quota per team/env (blue/green via namespaces). Admission controllers: intercept API requests (mutating/validating — PSA, LimitRange, Kyverno). Audit logs: apiserver audit policy records who did what — needed for compliance. PodSecurity: PSS profiles (baseline/restricted) enforced via PSA labels.",
 "1 = RBAC + namespaces. 2 = adds admission + PSA. 3 = ties audit logs to compliance and least-privilege SA design."),
("Operators: custom resources + control loops; controller pattern",
 "Operator = CRD (declarative domain API) + controller (reconcile loop: observe → diff → act → requeue). Controllers are event-driven + periodic resync, idempotent, retry with backoff, report status. Built-in controllers (ReplicaSet etc.) are the pattern; operators extend it to databases/certs/messaging. Write one when lifecycle logic exceeds manifests (backup/failover/renewal), not for stateless apps.",
 "1 = CRD + controller. 2 = reconcile loop mechanics. 3 = when-to-build judgment + cost framing."),
("Concepts: Docker Swarm vs K8s, imperative vs declarative (kubectl apply), self-healing/reconciliation",
 "Swarm = simple, Docker-native, limited scheduling/HA; k8s = extensible, self-healing, the ecosystem standard. Declarative (kubectl apply, manifests in Git) = you state desired state, controllers converge; imperative (kubectl run/create, scripts) = you issue commands, easy to drift. Self-healing = controllers constantly reconcile actual → desired (replace crashed pods, reschedule, resync). Declarative + GitOps = auditable, reproducible.",
 "1 = Swarm vs k8s. 2 = declarative vs imperative with kubectl apply. 3 = reconciliation as the mechanism of self-healing."),
("Service mesh: Istio/Linkerd — sidecar injection, mTLS, traffic routing/shift; when a mesh beats NetworkPolicies alone (~+30% premium skill in India data)",
 "Both inject sidecars; Istio (Envoy, feature-rich: weighted routing, fault injection, tracing) vs Linkerd (light, fast, golden metrics + mTLS). Mesh wins over NetworkPolicies when you need: universal mTLS without app changes, L7 routing/canary by header/weight, per-service SLO metrics + tracing, or cross-cluster identity. Costs: sidecar CPU/RAM per pod, latency, control-plane ops. Adopt at ~20+ services or when compliance demands universal mTLS; otherwise NP + app mTLS + OTel suffices.",
 "1 = sidecar + mTLS basics. 2 = Istio vs Linkerd tradeoff. 3 = adoption threshold logic + cost math."),
("Autoscaling: HPA (metrics-driven) vs VPA (recommender) vs Cluster Autoscaler vs Karpenter — which layer handles what",
 "HPA scales pod count (CPU/custom metrics) — stateless services. VPA adjusts requests/limits (restarts pods) — stateful/bursty. Cluster Autoscaler adds/removes NODES on pending pods (node groups; minutes to react). Karpenter provisions nodes fast with bin-packing (instance-type aware). Layers: HPA (pods) + CA/Karpenter (nodes); VPA is orthogonal (resource sizing). Combine HPA + Karpenter for elastic stateless; avoid VPA+HPA on the same metric.",
 "1 = names the four. 2 = maps pod vs node layers. 3 = composition rules + reaction-time differences."),
("ServiceAccounts: how pods authenticate to the API server, RBAC rolebinding, securityContext + Pod Security Standards",
 "Pod mounts its SA's JWT (/var/run/secrets/.../token) — authenticates to apiserver; authorization via RoleBinding to that SA. securityContext sets pod/container security (runAsNonRoot, readOnlyRootFilesystem, capabilities, seccomp). PSS profiles (baseline/restricted) enforced via PSA labels enforce these constraints at admission. Disable automountServiceAccountToken for API-free pods.",
 "1 = token mount + RBAC. 2 = securityContext fields. 3 = PSA enforcement + automount tradeoff."),
("Practice environments: minikube vs kind vs k3s vs managed (EKS/AKS/GKE) — why a real cluster beats a playground",
 "minikube = single-node learning VM; kind = multi-node in Docker (CI/controller testing); k3s = lightweight single-binary (edge/raspberry); managed = real cloud control plane + integration (IAM/ALB/spot). Playgrounds teach k8s primitives but hide cloud-specific realities (LB provisioning, node autoscaling, IAM, upgrade cycles) — a managed or kubeadm cluster is where production behaviors surface.",
 "1 = lists options. 2 = picks by use case. 3 = explains what playgrounds hide (cloud integration gap)."),
("Operator pattern deep: CRDs (custom resource definitions), controller reconcile loop, Operator SDK; real examples (Prometheus Operator, Argo CD, Kyverno)",
 "CRD = new API type (apiserver stores it); controller reconciles desired vs current. Operator SDK / controller-runtime scaffold the controller (watches, RBAC, status). Prometheus Operator: ServiceMonitor CRs → scrape config; Argo CD: Application CR → Git sync; Kyverno: policies as CRs → admission. Deep point: operators replace imperative runbooks (scripts + cron fixes) with declarative self-healing state.",
 "1 = CRD + controller. 2 = SDK scaffolding + reconcile mechanics. 3 = real examples + the 'runbooks → operators' shift."),
("etcd backup & restore — how do you snapshot etcd and restore a cluster from it (etcdctl snapshot save/restore, frequency, off-cluster storage)?",
 "etcd is the cluster's source of truth — losing it without a backup = losing the cluster. Backup: `etcdctl snapshot save` against a member (kubeadm-managed: etcdctl from the static-pod; EKS managed: automatic but verify). Frequency: every cluster change that matters or daily minimum — snapshots are cheap; keep N (e.g. 7 daily + weekly) off-cluster (S3/object store, encrypted). Restore: stop API server + etcd, `etcdctl snapshot restore <file> --data-dir=<dir>` (creates a new member with new cluster ID), point the etcd manifest's data-dir at it, restart, verify with `etcdctl endpoint health` + `kubectl get nodes`. Practice restores — a snapshot you never restored is a rumor. Also: backup the PKI/certs alongside (restore needs them), and remember restore = last-resort rollback (you lose changes after the snapshot).",
 "1 = mentions snapshot save. 2 = frequency + off-cluster storage + restore steps. 3 = restore drill + PKI + last-resort-rollback framing.",
 "etcd backup is the CKA 'backup and restore' objective and the classic 'we lost the control plane' incident — a concrete drill answer is the senior tell."),
],
"B02": [
("Walk me through write → plan → apply — what happens at each step and what can go wrong?",
 "Write: author HCL (providers, resources, variables, outputs). init: downloads providers/modules, initializes backend (state location + locking). plan: reads config + current state, computes the diff (create/update/delete), shows it — no changes yet; wrong here: secrets can appear in plan output, or plan differs from apply if state changed between. apply: executes the plan in dependency order, updates state, saves it to the backend; can fail midway (partial apply — state records what succeeded; re-run to converge). Then destroy if needed. Key failure modes: drift between plan and apply (someone else applied), state lock held, provider API rate limits, destroyed-then-recreated resources with identity change.",
 "1 = names the three steps. 2 = describes plan as diff + state update at apply. 3 = explains partial apply, drift-between-plan-and-apply, and state locking.",
 "The core workflow question — interviews probe whether you understand state as the plan/apply bridge."),
("provider vs resource vs data source vs variable vs output — the dependency graph between them",
 "provider = the API client (aws, google, kubernetes) — configures how resources talk to the cloud; resources = managed objects (create/update/delete); data sources = read-only queries of existing state (current AMI, existing VPC) used to inform resources; variables = inputs to the config (defaults, tfvars, env); outputs = exported values (IPs, ARNs) for reuse/other configs. Dependency graph: provider → resource/data source; resources reference each other implicitly (${aws_vpc.main.id} creates edges); variables feed resources; outputs read from resources/data sources; graph engine executes in parallel where independent.",
 "1 = defines the five. 2 = explains implicit references build the graph. 3 = covers parallel execution, depends_on, and data-source reads."),
("Terraform vs OpenTofu: what changed after the BSL relicensing and why does it matter?",
 "In Aug 2023 HashiCorp relicensed Terraform from MPL 2.0 to BUSL (Business Source License) — source-visible but NOT open-source; it restricts commercial use and competing products. OpenTofu: a fork (backed by Linux Foundation) keeping the MPL license — open source, community-governed, mostly compatible CLI/state. Why it matters: license risk for vendors (can't embed Terraform commercially), vendor lock-in concerns, and community governance. Practical: features diverge slowly; evaluate by your needs — OpenTofu for open-source guarantees, Terraform for HashiCorp ecosystem support (Cloud, Sentinel). State files are compatible either way.",
 "1 = knows BSL happened. 2 = explains BUSL vs MPL + OpenTofu fork. 3 = gives migration/compatibility guidance and ecosystem tradeoff."),
("How does Terraform know what to change (state diff)? What does plan output actually tell you?",
 "Terraform compares desired state (config) vs current state (state file, refreshed with real-world API data during plan). The diff drives the plan: + create, ~ update in place, - destroy, -/+ replace (destroy+recreate). Plan output = proposed actions + the attribute-level changes (what field differs and its old/new value), plus the execution order and any 'forces replacement' markers. It does NOT execute anything — it's a prediction; apply re-runs the plan internally to guard against drift.",
 "1 = says 'compares config to state'. 2 = explains plan actions (+/~/-/-/+). 3 = covers refresh, forces-replacement, and plan-as-prediction semantics."),
("What is terraform state and why does it matter? What breaks if you lose it?",
 "State = Terraform's mapping of config resources → real-world resource IDs + attributes. It's the source of truth for what Terraform manages. Without it: Terraform can't know what exists — plan shows everything as 'create' → re-creating resources (new IDs, data loss risk, orphaned old resources). State also stores metadata (dependencies, sensitive values in some cases). Mitigations: remote backend (S3 + DynamoDB) for durability + locking; `terraform state pull/push` for recovery; import to rebuild state from existing resources. Losing state of managed infra is the classic 'did I just delete my prod' scenario — a remote backend is non-negotiable.",
 "1 = says state maps config to real resources. 2 = explains loss consequences (recreate vs import). 3 = covers remote backend + import recovery and locking."),
("Remote backend: S3 + DynamoDB locking — what does each provide and why both?",
 "S3 backend: stores the state file durably + versioned (S3 versioning = accidental-corruption rollback), encrypted (SSE), accessible by the whole team. DynamoDB: provides state LOCKING — a lock table where each apply acquires a lock (prevents two applies racing and clobbering state); without it, concurrent applies corrupt state. Why both: S3 for storage + durability, DynamoDB for mutual exclusion — one without the other leaves a gap (unlocked concurrent applies destroy state integrity; state without versioning can't recover). Also: S3 key per workspace/environment, encryption + least-privilege IAM on both.",
 "1 = S3 stores state. 2 = adds DynamoDB locking. 3 = explains why BOTH are needed (durability vs concurrency) + versioning/encryption."),
("When do you use terraform import vs state rm vs state mv? Give real scenarios",
 "import: adopt an EXISTING resource into state (created via console/other tools) so Terraform manages it — e.g. import an old EC2/S3 bucket into config. state rm: unmanage a resource (keep it alive in the cloud but remove from state) — e.g. decommission a resource from Terraform's control, or after import mistakes. state mv: move a resource within state — rename, move between modules (refactor module structure), or move from state rm'd location; critical for refactors so you don't destroy-recreate. Real scenario: consolidating two modules → state mv old.module.res to new.module.res; adopting a manually created S3 bucket → import; stopping Terraform from deleting a legacy resource → state rm (then manage manually).",
 "1 = knows import exists. 2 = distinguishes import (adopt) vs rm (unmanage) vs mv (move). 3 = gives refactor + adoption scenarios and the destroy-prevention reasoning."),
("Sensitive state: how do you keep secrets out of the state file and plan output?",
 "State file stores ALL attribute values including secrets (plaintext in state, even if marked sensitive). Mitigations: 1) encrypt state at rest (S3 SSE, Terraform Cloud); 2) restrict state access via IAM (state contains secrets — least-privilege read); 3) avoid putting secrets in config: use variables with sensitive = true (still lands in state) or better, don't store at all: aws_ssm_parameter / aws_secretsmanager_secret data sources, or vault provider — secret fetched at apply, referenced, not embedded; 4) mark sensitive on outputs to redact plan/console display (doesn't remove from state); 5) rotate secrets and audit state access. Honest answer: if a secret must exist in config, it exists in state — reduce exposure (encryption, IAM, avoiding secrets in state) rather than claim you can exclude them.",
 "1 = knows secrets live in state. 2 = lists encryption + sensitive flag. 3 = explains data-source indirection (SSM/Secrets Manager) and the 'still in state' reality."),
("What makes a good module — inputs, outputs, versioning, and when is a module overkill?",
 "Good module: narrow purpose (one thing well), clear inputs (typed variables with descriptions + defaults), minimal required inputs (sensible defaults), explicit outputs (what consumers need), versioned + pinned, tested, documented. Naming convention terraform-<provider>-<name> (registry). Overkill when: single-use wrapper around one resource (a module for an SG with one rule), premature abstraction, or config that varies more than it repeats. Rule: a module earns its cost at 2+ reuse sites or when it encodes policy (compliance-standard VPC).",
 "1 = inputs/outputs. 2 = versioning + naming + testing. 3 = the reuse-threshold judgment and policy-encoding value."),
("How do you version modules (registry, Git tags) and pin them safely?",
 "Registry modules: semver versions (v1.2.0); public registry resolves automatically; private via registry mirror or git. Git modules: source = 'git::https://...//modules/vpc?ref=v1.2.0' — ref can be a tag (recommended), branch (dev only), or commit SHA (pinning). Pin safely: use tags/SHAs, never floating branches in prod; update deliberately (review changelog, run plan, test). Registry: constraint strings like '~> 1.2' allow minor upgrades; '>= 1.0, < 2.0' looser. Document upgrade cadence + breaking-change policy.",
 "1 = knows registry + git tags. 2 = explains ref pinning and constraints. 3 = the deliberate-upgrade workflow and branch-vs-tag warning."),
("count vs for_each — when each, and how do you reference their resources?",
 "count: creates a LIST of resources by integer index — use when instances are homogeneous and indexed (0..n). for_each: creates a MAP keyed by a unique key (string/set) — use when instances have distinct identities (per-team, per-env, name-derived) and stable keys matter. References: count → aws_instance.web[0], aws_instance.web.*.id; for_each → aws_instance.web[\"api\"].id, values(aws_instance.web), keys(). Why for_each over count: removing an item from the middle of a count list shifts indices → destroys/recreates unrelated resources; for_each keys are stable. Also: count can't create heterogeneous instances easily; for_each can via each.value.",
 "1 = knows both exist. 2 = explains list-vs-map and index shifting. 3 = the middle-removal destroy problem and keyed stability argument."),
("How do you share modules across teams without publishing publicly?",
 "Options: 1) private Git repos + git:: source refs (simplest — works with any Git host; pin with ref); 2) private module registry (Terraform Cloud/Enterprise, or self-hosted like Atlantis/Terraform Registry mirror); 3) git submodules/monorepo sharing via relative paths; 4) internal registry (JFrog, GitLab module registry). Best practice: private Git + tags + a registry layer if teams need discoverability/version constraints. Add CI on the module repo (terraform validate, tflint, checkov, plan against a fixture) so shared modules are tested before teams consume them. Governance: keep modules in a 'platform' org, PR review, semver.",
 "1 = says 'private git'. 2 = adds registry options + versioning. 3 = CI-on-modules + governance model."),
("What checks run before apply in a good pipeline (fmt, validate, plan review, tflint, checkov)?",
 "Sequence: 1) terraform fmt -check (style); 2) terraform validate (syntax/semantic); 3) tflint (provider-specific lint, deprecated args); 4) checkov/tfsec (security policy: public SG, unencrypted, IAM wildcards); 5) terraform plan against the right workspace/env; 6) human plan review (approval gate on the diff — especially destroys); 7) optionally policy-as-code (Sentinel/OPA) on plan; 8) apply with auto-approve ONLY after gating, or with explicit confirmation. The plan review is the real gate — the diff is the artifact people approve.",
 "1 = fmt + validate + plan. 2 = adds tflint/checkov + review gate. 3 = orders the full pipeline and explains WHY plan review is the critical control."),
("Someone ran apply directly against prod — how do you prevent it (state locking, approval, plan-as-code)?",
 "Prevention layers: 1) remote state + locking (S3+DynamoDB) so ops see conflicts; 2) separate workspaces/environments (dev/stage/prod) with IAM restricting who can access prod state/creds; 3) pipeline-only applies: prod apply runs only in CI with protected-branch triggers + manual approval (GitHub Environments / GitLab protected env); 4) no local credentials for prod (assume-role MFA, short-lived); 5) plan-as-code: CI produces the plan artifact, a human reviews and approves, then apply uses THAT plan (not a fresh local one); 6) audit logs (who ran what); 7) drift/change alerts. Culture: make 'apply directly' impossible, not just discouraged.",
 "1 = mentions locking. 2 = adds environment separation + approval. 3 = the plan-artifact + protected-env + no-local-prod-creds design."),
("What's the difference between apply -replace and taint, and why is -replace preferred?",
 "taint: marks a resource for destruction+recreation on the NEXT apply (deprecated since v0.15.2 — HashiCorp recommends apply -replace instead, because the change is visible in the plan; taint mutates state directly and other users can plan against it before you review). apply -replace <address>: destroys and recreates the specified resource in the same run, without a separate state mutation step. Why -replace preferred: taint required a separate state-mutating command (confusing, could be forgotten, race-prone); -replace is explicit in the apply invocation, targeted, and doesn't persist state changes. Also: -replace is the modern recovery for 'this resource is in a broken state, rebuild it' without editing config.",
 "1 = knows both exist. 2 = explains replace = destroy+recreate same run. 3 = the deprecation story + why -replace is safer/cleaner."),
("Drift: someone changed infra in the console — how do you detect and reconcile it?",
 "Detect: 1) plan output shows diffs vs state (Terraform's refresh detects drifted attributes); 2) terraform plan in CI on schedule (drift detection runs); 3) tools: driftctl, Atlantis drift detection, Terraform Cloud drift detection; 4) cloud-side: AWS Config/CloudTrail for change history. Reconcile options: a) accept the drift (update config to match reality — then import or edit config); b) remove the drifted resource from management (state rm) if it's now managed elsewhere; c) re-apply (terraform apply) to restore desired state (destructive to the console change — warn the team); d) document + follow up. Process: treat drift as an incident-lite: investigate the change (who/what via CloudTrail), decide intent, align config or state.",
 "1 = says 'plan shows it'. 2 = adds scheduled drift detection. 3 = the reconcile decision tree (align config vs re-apply vs unmanage) + change audit."),
("Core workflow: write -> plan -> apply; providers, resources, data sources",
 "Write HCL → init (providers/backend) → plan (diff config vs state) → apply (execute, update state). Providers = API clients; resources = managed objects; data sources = read existing state. State is the bridge: plan computes changes, apply persists them.",
 "1 = steps only. 2 = adds provider/resource/data roles. 3 = state-as-bridge + failure modes."),
("Commands: init/validate/plan/apply/destroy/fmt; apply -replace (preferred over deprecated taint)",
 "init: providers/backend/modules; fmt: style; validate: syntax; plan: diff; apply: execute; destroy: teardown (careful). -replace <addr> destroys+recreates one resource (modern replacement for the removed taint).",
 "1 = lists commands. 2 = maps each to its phase. 3 = -replace semantics + destroy safety."),
("State: tfstate, remote backend (S3 + DynamoDB locking), state file locking, sensitive state, refresh vs plan",
 "tfstate = config→real-world mapping. Remote backend: S3 (durable, versioned, encrypted) + DynamoDB (lock). Sensitive state: encrypt at rest + restrict IAM + avoid embedding secrets (data sources). refresh = re-read reality; plan = refresh + diff.",
 "1 = defines state. 2 = S3+DynamoDB rationale. 3 = secret-in-state reality + refresh-vs-plan."),
("Modules: terraform-<PROVIDER>-<NAME> naming, registry, outputs -> variables wiring, versioning",
 "Registry naming terraform-aws-vpc; modules wire via outputs → consumers' variables; version with semver tags; pin refs (~> 1.2 / git ref). Good module: small scope, typed inputs, explicit outputs, tested, documented.",
 "1 = naming + registry. 2 = output→variable wiring. 3 = versioning discipline + when modules are overkill."),
("Idempotency/rollback: recommit old code (Terraform has no built-in rollback), import existing resources",
 "Terraform is idempotent (re-apply converges) but has NO rollback — reverting means recommitting old config and re-applying (which may destroy newer resources). Import adopts existing resources into state. Rollback strategy: keep previous state/plan artifacts; use -replace for bad single resources; for real rollback, apply the last-known-good config.",
 "1 = knows no rollback. 2 = recommit + reapply as rollback. 3 = import + plan artifacts + -replace recovery."),
("Advanced: null_resource, count vs for_each, terraform_workspace vs terraform_remote_state, Terragrunt (DRY, immutable versioned modules)",
 "null_resource + local-exec/provisioner = side effects (scripts) — use sparingly, they're not tracked like resources. count = indexed list; for_each = keyed map (stable keys). terraform_workspace = local state separation (dev/prod states); terraform_remote_state = read another config's outputs (cross-config data). Terragrunt: DRY wrapper — shared backend config, module version pinning, dependency orchestration.",
 "1 = names the tools. 2 = explains each purpose. 3 = when to reach for Terragrunt + workspace vs remote_state."),
("Comparisons: vs Ansible (provision vs config, declarative vs procedural), vs CloudFormation (multi-cloud, HCL vs JSON/YAML, state)",
 "Ansible = config management + provisioning (procedural tasks, SSH push, agentless) — configures EXISTING machines; Terraform = declarative provisioning (state, destroy) — creates the machines. Often paired (TF for infra, Ansible for config). CloudFormation = AWS-only, JSON/YAML, state managed by AWS (stacks), similar declarative model; Terraform = multi-cloud + HCL + own state/backend. Choose TF for multi-cloud/portable; CF for deep AWS-native integration.",
 "1 = TF vs Ansible one-liner. 2 = declarative vs procedural + pairing. 3 = CF comparison + multi-cloud/state angle."),
("Terraform Cloud/Enterprise: workspaces, policy-as-code (Sentinel/OPA), runs, VCS-driven",
 "Terraform Cloud: managed runs (plan/apply in cloud), remote state, workspaces per env/team, VCS-driven (PR → plan → comment → apply on merge), policy-as-code via Sentinel (HCL policies) or OPA (rego), cost estimation, private registry. Enterprise adds SSO, audit, agents. Value: enforces plan-review gates + policy checks centrally.",
 "1 = workspaces + VCS. 2 = policy-as-code + runs model. 3 = the governance value (plan gates, Sentinel/OPA, audit)."),
("Request flow architecture: CLI -> provider -> API; graph execution, parallelism",
 "terraform CLI → provider plugin (gRPC) → cloud API. Graph: resources/nodes + dependency edges (implicit refs, depends_on); plan/apply traverse the graph in topological order, executing independent branches in parallel (default parallelism 10). Cycles (a depends on b, b on a) are errors.",
 "1 = CLI→provider→API. 2 = graph + topological order. 3 = parallel execution + cycle errors + parallelism tuning."),
("Pulumi vs Terraform — what does IaC in a real programming language (TypeScript/Python) give you, and when would you choose it?",
 "Pulumi: IaC in real languages (TS, Python, Go, C#) — you write code, not HCL: loops/conditionals/classes natively, shared libraries, IDE type-checking, unit tests of your infra logic. Same declarative model + state (per-project stack, stored in a backend — S3/self-hosted/Pulumi Cloud). Terraform: HCL, the ecosystem default, huge provider/module registry, HashiCorp tooling (Cloud, Sentinel). Choose Pulumi when: dev-heavy team (reuse existing code skills), complex logic (per-env branching, generated config), or you want testable infra; choose Terraform when: HCL is fine, you want the largest provider/module ecosystem and hiring pool, or the org already runs it. Both support the same clouds; migration cost (state + HCL→code rewrite) is the real barrier.",
 "1 = names Pulumi. 2 = code-vs-HCL + state model. 3 = ecosystem vs language-power tradeoff + migration-cost honesty.",
 "Pulumi is the 'awareness' question — roadmap.sh lists it alongside Terraform; interviewers check you know when language-native IaC wins."),
("Infracost — how do you estimate a plan's cost before apply and gate expensive changes in CI?",
 "Infracost parses a Terraform/OpenTofu plan (or runs its own) and estimates monthly $ by resource, using current cloud pricing. CI integration: run `infracost breakdown` on the plan in a pipeline step → comment the diff on the PR ('this change adds $124/mo — 3 EC2 t3.medium + NAT'). Gating: either informational (comment) or enforced — a workflow that fails the PR when the delta exceeds a budget (e.g. > $200/mo) or when specific expensive resources are added (nat_gateway, multi-AZ RDS). Config: usage-file (actual quantities: GB stored, data transfer) makes estimates accurate; per-workspace config. Value: cost awareness at design time — the PR review becomes the cost checkpoint, catching expensive mistakes before apply.",
 "1 = 'estimates plan cost'. 2 = CI PR comment + how it works. 3 = budget gating + usage-file accuracy + cost-review-as-standard-practice.",
 "Infracost is the FinOps-meets-IaC question — mid-level interviews increasingly ask how cost gates fit into plan/apply discipline."),
],
"B03": [
("EC2 instance families — how do you pick the right one for a workload (general/compute/memory/storage/accelerated)?",
 "Families: general (M/T) — balanced, default for web/app; compute (C) — CPU-heavy (batch, transcoding); memory (R/X) — large RAM (DBs, caches, in-memory); storage (I/D/H) — high local I/O (databases, big data); accelerated (P/G/Inf) — GPU/FPGA (ML training/inference); burstable (T) — variable CPU with CPU credits (dev, spiky). Pick by the bottleneck: measure CPU/memory/IO/GPU utilization; choose the family matching the dominant resource; right-size within family (instance size = vCPU/RAM). Rule: profile first (CloudWatch metrics), then pick family, then size.",
 "1 = names a few families. 2 = maps workload → family. 3 = metric-driven selection + right-sizing loop."),
("On-demand vs spot vs reserved/Savings Plans — cost/latency tradeoffs and when spot is acceptable",
 "On-demand: full price, no commitment, instant — for unpredictable/required-uptime. Spot: up to ~90% cheaper, but instances can be reclaimed (2-min warning) — for fault-tolerant, interruptible workloads (batch, CI, stateless workers, ML training with checkpointing). Reserved (1-3yr) / Savings Plans (commit $/hr): up to ~70% off — for steady-state baselines (prod DBs, always-on). Spot acceptable when: work can restart (idempotent), no state on instance, workload tolerates interruption — never for stateful single instances. Design: spot for elastic + reserved/SP for baseline + on-demand for overflow.",
 "1 = knows spot is cheaper but interruptible. 2 = maps workload classes to each. 3 = the mix design (spot elastic + SP baseline) + reclamation handling."),
("stop vs terminate — what survives each? EBS volume lifecycle and snapshots",
 "stop: instance halts, EBS volumes (root+data) persist, public IP may change (private IP persists), billed for storage not compute; start resumes. terminate: instance and its root EBS volume (deleteOnTermination) are destroyed — data gone unless you set deleteOnTermination=false or snapshot. Survives stop: EBS data, private IP (for stopped), AMI unchanged; survives terminate: snapshots, AMIs, and detached volumes with deleteOnTermination=false. Lifecycle: snapshot EBS for durability (backup, migration, restore). Rule: for durable data use separate EBS with deleteOnTermination=false + snapshots; instance-store data dies on stop/terminate.",
 "1 = stop keeps data, terminate deletes. 2 = EBS persistence + deleteOnTermination. 3 = snapshot strategy + instance-store caveat."),
("S3 storage classes + lifecycle rules — when does cold storage make sense?",
 "Classes: Standard (hot, low latency, frequent access), Intelligent-Tiering (auto moves by access pattern, monitoring fee), Standard-IA (infrequent, retrieval fee), OneZone-IA (single AZ, cheaper, less durable), Glacier Instant/Glacier Flexible (archive, minutes-to-hours retrieval), Glacier Deep Archive (coldest, ~12h retrieval, cheapest). Lifecycle rules transition objects by age (e.g. 30d → IA, 90d → Glacier, 365d → Deep Archive, 730d → expire). Cold storage makes sense for: compliance archives, backups, logs with retention mandates, old versions — where access is rare and retrieval latency acceptable. Cost analysis: compare storage savings vs retrieval + minimum storage durations.",
 "1 = lists classes. 2 = lifecycle transition example. 3 = cost/retrieval tradeoff + when cold is wrong (frequent reads)."),
("EBS vs EFS vs S3 — pick storage for a stateful app and justify",
 "EBS: block storage attached to ONE EC2 (low latency, high IOPS) — databases, stateful app data. EFS: NFS file system shared across many instances (multi-AZ, POSIX) — shared files, web farms, content. S3: object storage, HTTP API, unlimited scale, strongly consistent — backups, media, static assets, data lakes. Decision rule: single-node latency-sensitive → EBS; multi-node shared files → EFS; object/archive/unstructured → S3. Justify with: access pattern (block vs file vs object), sharing needs, scale, latency budget, cost.",
 "1 = knows the three exist. 2 = block vs file vs object mapping. 3 = justified pick with access-pattern reasoning + cost/latency."),
("SQL vs NoSQL — how do you decide for a new service? Give a decision rule, not a preference",
 "Decision rule: need ACID transactions, joins, ad-hoc queries, fixed schema, strong consistency → relational (Postgres). Need horizontal scale, flexible schema, high write throughput, document/graph/key-value model → NoSQL (DynamoDB/Mongo). More precisely: if the access pattern is known and queryable-by-key or by-document → NoSQL wins on scale; if queries are unpredictable/analytic or data is highly relational → SQL. Start relational unless you have a concrete scale/schema-flexibility need — Postgres scales further than people think; NoSQL is chosen for a reason (throughput, model fit), not fashion.",
 "1 = names tradeoffs. 2 = gives a rule (transactions/joins → SQL). 3 = nuanced: known access pattern → NoSQL; relational default + scale reasoning."),
("A query is slow in production — what's your diagnosis order (EXPLAIN, indexes, connection pool, locks)?",
 "Order: 1) confirm it's the query (app timing, query logs); 2) EXPLAIN / EXPLAIN ANALYZE — plan, seq scan vs index, row estimates vs actual, joins; 3) missing/wrong index → add/composite index; 4) check slow query log + stats (perf insights / pg_stat_statements); 5) connection pool — pool exhaustion (too many connections, pool too small, leaked connections) can make queries 'slow'; 6) locks/blocking — waiting on row/table locks (pg_locks, MySQL innodb status); 7) resource contention (CPU/IO) — sometimes the DB is fine, the box isn't. Fix smallest first: index → query rewrite → schema → hardware. Verify with the same EXPLAIN after.",
 "1 = says EXPLAIN. 2 = index + pool order. 3 = full order incl. locks + contention + verify-after-fix."),
("Walk me through a restore drill: snapshot → PITR → verify — and how it maps to your RTO/RPO",
 "Drill: 1) define targets first — RPO (acceptable data loss, e.g. 5 min) and RTO (acceptable downtime, e.g. 1 hr); 2) take a base snapshot (RDS snapshots are transaction-consistent; EBS snapshots + DB-level backup for multi-volume); 3) enable PITR (RDS automated backups = snapshots + transaction logs → restore to any point); 4) restore to a NEW instance (never overwrite prod) — snapshot restore (point-in-time of snapshot) or PITR restore (chosen timestamp within retention); 5) verify: connect, check schema, row counts, recent transactions, app smoke test; 6) cut over via DNS/route change (update endpoint). Measure: RPO achieved = backup interval/log retention; RTO achieved = restore time + verification + cutover. The drill exposes what doc-compliant RTO/RPO claims hide.",
 "1 = knows snapshot restore. 2 = PITR + new-instance + verify. 3 = maps drill timing to measured RTO/RPO and tests cutover."),
("VPC anatomy: subnets/AZ, IGW, NAT, route tables — draw the flow of a request in vs out",
 "VPC = isolated network (CIDR, e.g. 10.0.0.0/16); AZs = failure domains; subnets = AZ-scoped slices (public: route 0.0.0.0/0 → IGW; private: no IGW route, egress via NAT). IGW = internet gateway (public inbound/outbound); NAT gateway = outbound-only from private subnets; route tables = per-subnet routing decisions; security groups attach at instance level. INBOUND to public app: internet → IGW → route table → public subnet → SG allows 443 → instance. OUTBOUND from private app (e.g. calling an API): instance → route via NAT GW → IGW → internet. DB in private subnet: no inbound from internet; only app SG allows 3306.",
 "1 = names components. 2 = traces a request path in/out. 3 = public vs private routing + NAT direction and SG scoping."),
("Security Groups vs NACLs — stateful vs stateless, and why SG rules are inbound-only",
 "SG: instance-level virtual firewall, STATEFUL (return traffic auto-allowed), rules evaluated as a set (allow-list), no deny rules (implicit deny). NACL: subnet-level, STATELESS (must allow BOTH directions explicitly), numbered rules with allow/deny, evaluated in order. SG has no explicit deny → the 'inbound-only' phrasing is a trap: SG rules are direction-specific (inbound rules + outbound rules are separate lists) but return traffic is handled automatically (stateful). Use SGs as primary (they're the AWS best practice), NACLs as an extra layer for subnet-wide deny (e.g. block an IP range, block specific ports at subnet boundary).",
 "1 = SG stateful vs NACL stateless. 2 = explains direction + implicit deny. 3 = the stateful-return-traffic detail + when NACLs add value."),
("How do you diagnose a blocked connection (VPC Flow Logs, SG/NACL rules, reachability analyzer)?",
 "Order: 1) reproduce + identify source/dest/port; 2) VPC Reachability Analyzer (AWS) — path-based check, tells you exactly which SG/NACL/route blocks it; 3) SG rules — is the port allowed on source AND dest SGs (both directions matter); 4) NACLs — stateless, check inbound AND outbound rules; 5) VPC Flow Logs — did packets reach the interface? ACCEPT vs REJECT records + the rule that matched; 6) routing (missing route, IGW/NAT state); 7) also verify: instance listening? (netstat/ss), service healthy. Flow logs answer 'did the packet arrive and why was it dropped'; reachability analyzer answers 'which rule blocks it'.",
 "1 = checks SG. 2 = adds NACL + flow logs. 3 = reachability analyzer + reading ACCEPT/REJECT + full stack."),
("VPC peering vs Transit Gateway vs VPN — when each",
 "VPC peering: direct private connection between 2 VPCs (no transitive routing — must peer each pair; for a hub-and-spoke of N VPCs you need N-1 peerings). Transit Gateway: hub router connecting many VPCs + on-prem (transitive, central route control, scales to hundreds of VPCs) — the standard for multi-VPC/org networking. VPN (Site-to-Site): encrypted tunnel over internet to on-prem — for hybrid, no Direct Connect needed; slower/latency. When: 2 VPCs only → peering; many VPCs or org-wide → TGW; on-prem/hybrid → VPN (or Direct Connect for low-latency dedicated). Also: peering has no transitive routing — the gotcha that pushes teams to TGW.",
 "1 = knows peering + VPN. 2 = TGW hub for scale. 3 = peering's no-transitive gotcha + hybrid choice."),
("ALB vs NLB vs CLB — protocol, features, and when you'd pick each",
 "ALB (L7): HTTP/HTTPS/gRPC/WebSocket, path/host routing, target groups, sticky sessions (cookie), WAF integration, slow start — for HTTP apps needing routing rules. NLB (L4): TCP/UDP/TLS passthrough, extreme throughput, static IPs, preserve client IP, handles millions of rps — for raw TCP, non-HTTP protocols, or when ALB L7 overhead is unwanted. CLB (classic): legacy, both L4+L7, no modern features — only for old stacks. Pick: HTTP/gRPC with routing → ALB; TCP/UDP/performance → NLB; EKS internal services → NLB for NodePort or ALB via ingress.",
 "1 = names the three. 2 = L7 vs L4 mapping. 3 = feature-driven pick (routing vs throughput) + EKS/ingress context."),
("How does an ASG work with an ELB — health checks, cooldown, lifecycle hooks, scaling policies",
 "ASG manages instance count; ELB distributes traffic to the ASG's instances (target group). Health checks: ELB health check (HTTP/TCP) → unhealthy instances deregistered + replaced by ASG (ELB health is used when configured as the ASG health check type). Cooldown: delay after a scaling action before another (prevents oscillation) — replaced by instance warm-up with target tracking. Scaling policies: manual, scheduled, simple (threshold), step, or target-tracking (best: e.g. keep CPU @ 50%); scale-out fast, scale-in slow (hysteresis) to avoid thrash. Lifecycle hooks: pause instances during scale-in/out to run custom actions (drain, register with CMDB, run final scripts). Launch template + min/max/desired define capacity.",
 "1 = ASG + ELB pair instances. 2 = health-check-driven replacement + cooldown. 3 = target-tracking + lifecycle hooks + slow scale-in."),
("Target groups, sticky sessions, and connection draining — what problem does each solve?",
 "Target groups: logical group of instances (by port/protocol) that a listener routes to — enables path-based routing to different backends (e.g. /api → API group, /static → web group) and health checks per group. Sticky sessions: pin a client to the same instance via cookie — for session state not in a shared store (caveat: unbalanced traffic; prefer stateless + shared sessions). Connection draining: when an instance deregisters (scale-in/replace), ELB stops new connections but lets in-flight requests finish (deregistration delay, default 300s) — prevents cutting active requests during deploys/scaling. Together: healthy routing (groups), consistent session (sticky), graceful removal (draining).",
 "1 = one-line each. 2 = explains the problem each solves. 3 = sticky-vs-shared-session tradeoff + draining during deploys."),
("Route 53 routing policies: simple, weighted, latency, failover — give a use case for each",
 "simple: single record → one target (one web server). weighted: distribute % across targets (canary 5% → new version; A/B testing). latency: route to lowest-latency region (global app across US/EU/AP — users hit nearest). failover: primary/backup with health checks (active-passive DR — fail to standby region when primary health check fails). geolocation: route by user location (compliance, local content). Also: multivalue (random healthy), geoproximity (traffic flow + bias). Use alias records for AWS resources (ELB/S3/CloudFront) — free, no extra DNS charge, auto IP changes.",
 "1 = names policies. 2 = matches a use case to each. 3 = alias-vs-CNAME + canary weighted + DR failover design."),
("IAM policy structure: Effect/Action/Resource/Condition — write a least-privilege S3 policy from scratch",
 "Structure: {\"Version\": \"2012-10-17\", \"Statement\": [{\"Effect\": \"Allow\", \"Action\": [\"s3:GetObject\"], \"Resource\": [\"arn:aws:s3:::my-bucket/*\"], \"Condition\": {\"IpAddress\": {\"aws:SourceIp\": \"10.0.0.0/8\"}}}]}. Least-privilege S3: allow only the actions needed (s3:GetObject, not s3:*), scope Resource to the bucket + prefix (arn:...:my-bucket/app/*), add conditions (IP range, Requester pays, VPC endpoint), Deny explicit where needed (e.g. deny s3:PutObject without encryption: Condition StringNotEquals s3:x-amz-server-side-encryption AES256). Prefer managed policies for common jobs; inline for unique. Key principles: deny-by-default, narrow actions/resources, least-privilege conditions.",
 "1 = Effect/Action/Resource. 2 = scopes resource + condition. 3 = least-privilege example + encryption condition + deny pattern."),
("Roles vs users vs instance profiles — when does an EC2 get credentials and how?",
 "Users: long-lived creds for humans (access keys, MFA). Roles: identity for entities to ASSUME (no long-lived keys; temporary creds via STS, 15min-12h). Instance profiles: the container that lets an EC2 instance assume a role — you attach an instance profile to the instance; the instance gets temporary creds via the metadata service (IMDSv2 recommended). The EC2 should NEVER have access keys — it assumes a role via its profile. When: humans → users (with MFA); EC2/services → roles (least-privilege per instance); cross-account → assume-role. Pattern: web instances get a role scoped to read their bucket; DB instances a role for backups.",
 "1 = users vs roles basics. 2 = instance profile + metadata creds. 3 = no-access-keys-on-EC2 rule + IMDSv2 + least-privilege roles."),
("How do you scope an IAM role for a CI/CD pipeline (short-lived creds, external ID)?",
 "CI/CD needs temporary creds per job, scoped narrowly. Design: 1) OIDC federation — GitHub Actions/GitLab/GitHub OIDC provider: the pipeline assumes a role WITHOUT long-lived keys (trust policy checks the OIDC sub/aud claim → repo + branch/ref); 2) External ID (if a third party assumes the role) — a secret-ish string the trust policy requires, prevents confused-deputy; 3) role scoped to the job: deploy role can only write to the target env's resources (e.g. ECR push + specific S3 + ECS update), NOT full admin; 4) session duration + permissions boundary; 5) audit via CloudTrail. The answer to watch: no static AWS keys in CI secrets — OIDC federation is the modern pattern.",
 "1 = mentions short-lived creds. 2 = OIDC federation + scoped role. 3 = trust policy (sub/aud) + External ID + permissions boundary + CloudTrail audit."),
("What is the AWS shared responsibility model and where does IAM sit in it?",
 "AWS secures the CLOUD (physical data centers, hardware, network, managed service internals, hypervisor); customer secures IN THE cloud (OS, app code, data, network config, IAM, encryption of customer data). IAM is customer responsibility — identity and access management for users, roles, policies, MFA, and audit. For managed services (RDS, S3) AWS handles more (patching, storage), but access control to YOUR data is still yours. Know where each service falls: S3 = customer manages data/permissions; Lambda = AWS runtime, customer code/perms.",
 "1 = 'AWS physical, you logical'. 2 = IAM is customer-side + examples. 3 = per-service nuance (managed vs self-managed) + data classification."),
("Your bill doubled — walk me through your investigation order (Cost Explorer, tags, unused resources)",
 "Order: 1) Cost Explorer — daily spend by service (which service jumped); 2) group by linked account/region/tag to isolate; 3) drill to the resource: EC2 (unused instances, oversized), NAT GW hours (expensive!), data transfer (egress!), S3 (class transitions, versioning growth), RDS; 4) look for: instances left running after tests, forgotten dev environments, EIPs, unattached volumes, snapshots piling up, data egress spikes (NAT/CloudFront); 5) check anomaly alerts + budgets for recurrence. Communicate: exact service + region + time window + fix. Common culprits: NAT gateways, EBS snapshots, orphaned resources, spot termination refills, license/RI expiry.",
 "1 = opens Cost Explorer. 2 = service-level isolation + common culprits. 3 = full drill-down + recurrence prevention (alerts, cleanup automation)."),
("Right-sizing: how do you find over-provisioned instances and what's the fix?",
 "Find: CloudWatch metrics per instance (CPU/mem/network utilization over 14-30 days) + Trusted Advisor / Compute Optimizer (recommends size changes by utilization pattern). Look for: avg CPU < 10-20%, memory underused, spiky usage on T-series. Fix: 1) downsize (change instance type — requires stop/start or in-place for same family); 2) for spiky: burstable (T3/T4g) with credits; 3) consolidate multiple underused instances; 4) verify app behavior after resize (re-baseline); 5) automate: rightsizing reports monthly + budgets alert on spend. Compute Optimizer is the free, data-driven answer — use it over guesswork.",
 "1 = checks CPU. 2 = Compute Optimizer + CloudWatch window. 3 = resize decision (steady vs spiky) + verify-after + recurring review."),
("Spot/reserved/Savings Plans: how would you design a cost strategy for dev vs prod?",
 "Dev: spot-first (interruptible is fine for test/CI — use spot fleet/mixed ASG), T-series burstable for light work, stop non-work-hours (scheduling), no reserved commitment. Prod: baseline (always-on) → Savings Plans/Reserved (steady load), spot for elastic/fault-tolerant overflow (batch workers, canary pools with min on-demand), on-demand for the irreducible core. Global: tagging + budgets + anomaly alerts on both; rightsizing reviews monthly. The mix: cover predictable baseline with SP/RI (~60-70%), use spot for burst, keep small on-demand buffer.",
 "1 = spot dev + reserved prod. 2 = Savings Plans + scheduling dev. 3 = the percentage mix + tagging/budget guardrails + spot-overflow design."),
("How do you enforce tagging and budgets so costs don't recur (Budget alerts, anomaly detection)?",
 "Enforce tagging: 1) tag policies (AWS Organizations) — required tags (env, owner, cost-center) with allowed values; 2) deny un-tagged resource creation via SCP/IAM condition (aws:RequestTag / aws:ResourceTag) — or at least require env/owner; 3) automation to fix/flag missing tags (Config rule + remediation). Budgets: AWS Budgets (monthly + forecast) alerting at thresholds (50/80/100%+) to email/Slack/chatbot; anomaly detection (Cost Anomaly) for unexpected spikes; SCP to cap spending (hard limits) on dev accounts. Recurrence: weekly cost review, rightsizing reports, cleanup automation (idle resources), cost-allocation tags feeding dashboards.",
 "1 = budgets + alerts. 2 = tagging enforcement + anomaly detection. 3 = SCP deny + Config remediation + recurring review cadence."),
("Compute: EC2 (types by use: general/compute/memory/storage/accelerated; T2 burstable), on-demand vs spot vs reserved, stop vs terminate, key pairs, AMI components, private IP immutable",
 "EC2 families map to workload bottleneck (M general, C compute, R memory, I/D storage, G/P accelerated); T = burstable (CPU credits). Pricing: on-demand (flex), spot (90% off, reclaimable), reserved/SP (commit for baseline). stop keeps EBS, terminate destroys root volume. Key pairs = SSH auth; AMI = OS+config template (region-scoped); private IP stable while running (changes on stop/start unless ENI-attached/EIP).",
 "1 = family mapping. 2 = pricing + stop/terminate. 3 = burstable credits + AMI/region + IP immutability nuance."),
("Network: VPC (200 subnets max), subnets/AZ, IGW/NAT, security groups (stateful, inbound-only rules) vs NACLs (stateless, explicit in+out), CloudWatch + VPC Flow Logs for monitoring",
 "VPC = isolated network; subnets are AZ-scoped (max 200/VPC); IGW = internet gateway; NAT = outbound-only for private. SG = stateful instance firewall (allow-list, return auto-allowed); NACL = stateless subnet firewall (in+out explicit). Monitor with CloudWatch (network metrics) + VPC Flow Logs (ACCEPT/REJECT records for audit/debug).",
 "1 = VPC/subnet/IGW. 2 = SG vs NACL statefulness. 3 = flow logs + NAT direction + subnet-AZ design."),
("Storage: S3 storage classes (Standard/IA/RRS/Glacier), multipart upload >100MB, S3 as REST service, EBS vs instance store, connection draining (ELB)",
 "S3 classes: Standard (hot) → IA (infrequent, retrieval fee) → Glacier/Deep Archive (archive, minutes/hours). Multipart upload for >100MB (parallel, resumable); S3 is a REST service (HTTP API, no mounting). EBS = durable network block storage; instance store = ephemeral local (fast, lost on stop). Connection draining = ELB lets in-flight requests finish before deregistering an instance.",
 "1 = storage classes. 2 = multipart + EBS vs instance store. 3 = REST model + lifecycle + draining."),
("DNS/CDN: Route 53 (global DNS, nearest DC routing), CloudFront (Geo-Targeting)",
 "Route 53 = AWS DNS (global, latency/geolocation routing, health checks, alias records). CloudFront = CDN (edge caching, TLS termination, Geo-Targeting/restrictions, WAF integration) — put CloudFront in front of S3/ALB for latency + cost + DDoS protection.",
 "1 = what each is. 2 = routing policies + edge caching. 3 = geo-targeting + WAF + origin design."),
("IAM: roles, federated access, Power User vs Admin, password policies, least privilege",
 "Roles = assumed identity (temp creds); federated access = SSO/IdP (SAML/OIDC) letting corporate users assume roles; Power User = full services but NO IAM; Admin = IAM too; password policies = complexity/rotation/MFA enforcement; least privilege = minimal actions/resources, deny-by-default, reviewed. Apply: humans via federation + MFA, machines via roles, no shared keys.",
 "1 = roles vs users. 2 = federation + Power User vs Admin. 3 = least-privilege workflow + MFA + credential review."),
("Scaling/HA: AZ vs Region, autoscaling lifecycle hooks, consistency models (eventual vs strong), RTO/RPO",
 "AZ = failure-isolated data center (redundancy within region); Region = full independence (DR, compliance). Autoscaling: target-tracking + lifecycle hooks (drain/register). Consistency: strong (RDS, single-node reads) vs eventual (DynamoDB default, replication lag) — pick by correctness needs. RTO (time to recover) / RPO (data loss tolerance) define DR targets — cross-AZ for HA, cross-region for DR.",
 "1 = AZ vs region. 2 = autoscaling + consistency. 3 = RTO/RPO-driven DR design + hook use."),
("Service model: IaaS/PaaS/SaaS, Snowball (data transfer), CloudWatch alarms",
 "IaaS = raw compute/storage you manage (EC2, EBS); PaaS = managed platform (RDS, EKS, Lambda — you manage less); SaaS = fully managed product (S3, CloudFront, SES). Snowball = physical device for large data migration (petabytes over slow links). CloudWatch alarms = threshold alerts on metrics (with SNS actions) — the base monitoring primitive.",
 "1 = defines the three. 2 = example mapping. 3 = Snowball use case + alarm design (thresholds, SNS, auto-remediation)."),
("Cost & FinOps: right-sizing from CloudWatch metrics, spot vs reserved mix + Savings Plans, tagging strategy + cost allocation tags, AWS Budgets + anomaly detection alerts, idle cleanup (unattached EBS, unused EIPs, stopped instances)",
 "FinOps loop: right-size (CloudWatch/Compute Optimizer), mix (spot for elastic + SP/RI for baseline + on-demand buffer), tag everything (cost allocation tags → dashboards), budget alerts (thresholds + forecast) + anomaly detection, idle cleanup automation (unattached EBS, unused EIPs, stopped instances, old snapshots). Make cost a team metric via tagged dashboards + reviews.",
 "1 = right-sizing + spot. 2 = tagging + budgets. 3 = full FinOps loop + cleanup automation + team accountability."),
("ELB specifics: ALB vs NLB vs CLB, target groups, health checks, sticky sessions, connection draining",
 "ALB = L7 (HTTP routing, path/host, target groups); NLB = L4 (TCP/UDP, static IP, high throughput); CLB = legacy. Target groups = listener → backend sets with per-group health checks; sticky sessions = cookie-pinned affinity; connection draining = graceful deregistration (in-flight completes).",
 "1 = ALB vs NLB. 2 = target groups + health checks. 3 = sticky/draining semantics + pick criteria."),
("Route 53 routing policies: simple / weighted / latency / failover / geolocation, alias vs CNAME records",
 "simple = one target; weighted = % split (canary); latency = nearest region; failover = active/passive with health checks; geolocation = by user location. Alias records = AWS-native (free, auto IP updates, works at zone apex) vs CNAME (only subdomains, extra lookup, can't be apex).",
 "1 = names policies. 2 = use-case mapping. 3 = alias-vs-CNAME + health-check-driven failover."),
("IAM deep: policy structure (Effect/Action/Resource/Condition), inline vs managed policies, roles vs users, instance profiles, cross-account assume-role",
 "Policy = Effect/Action/Resource/Condition statements. Managed policies = reusable/versioned (AWS + customer-managed); inline = embedded in a user/role (unique, harder to audit). Users = long-lived humans; roles = assumed (temp creds); instance profiles attach roles to EC2. Cross-account: role in target account + trust policy allowing source account/principal to assume (with External ID for third parties).",
 "1 = policy structure. 2 = managed vs inline + instance profiles. 3 = cross-account assume-role flow + External ID + trust policies."),
("AWS Organizations & SCPs — how do SCPs act as guardrails that override IAM, and what's a multi-account OU strategy?",
 "AWS Organizations: manage many accounts centrally — OUs (organizational units) group accounts (prod/dev, business units); SCPs (Service Control Policies) are guardrails applied at the org/OU/account level that OVERRIDE IAM: they cap the maximum permissions anyone in that account (including root) can have. E.g. SCP 'deny disabling CloudTrail', 'deny public S3 buckets in prod OU', 'deny leaving the org'. SCPs are not grants — they subtract from IAM's effective permissions (IAM allow ∩ SCP allow = effective). Multi-account strategy: separate accounts per env (dev/stage/prod) + security/logging account + sandbox; OUs per business unit; guardrails per OU (prod stricter: no root keys, MFA required, public access denied); centralized CloudTrail/Config in the audit account; cross-account roles for access. Why it matters: IAM alone can't enforce org-wide policy — a developer with account admin can still do anything in their account; SCPs make the guardrail structural.",
 "1 = 'SCPs override IAM'. 2 = OU hierarchy + deny examples. 3 = IAM∩SCP effective-permission model + full multi-account architecture.",
 "Multi-account strategy is standard enterprise AWS — SCP-as-guardrail is exactly what mid-level interviews probe beyond single-account IAM."),
],
}
